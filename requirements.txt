# OceanEmbed (PS 26066) -- pinned-loose requirements for both Shivansh's
# data pipeline (src/) and Satyam's training notebook (notebooks/)

# Data handling
xarray>=2024.3.0
netCDF4>=1.6.5
numpy>=1.26
pandas>=2.2
scipy>=1.12

# Regridding (optional but recommended for SST/SSS conservative regrid)
# xesmf>=0.8   # requires ESMF -- install via conda: `conda install -c conda-forge xesmf esmpy`

# Data access
copernicusmarine>=1.3.2   # CMEMS: OSTIA SST, SSS, DUACS SSH, GLORYS temperature
earthaccess>=0.9.0        # NASA Earthdata / PO.DAAC: OSCAR currents, CCMP winds

# Modeling
tensorflow>=2.15
scikit-learn>=1.4

# Plotting / notebook
matplotlib>=3.8
jupyter>=1.0
pyarrow>=15.0              # for parquet export (optional)
